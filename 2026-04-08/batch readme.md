1. Функция read_invoice не проверяла FileNotFoundError. Он также читал общие файлы, а не конкретно PDF
2. Функция count_pdfs проверяла .pdf, но пропустила бы .PDF
3. Функция get_supplier предполагала что все файлы имели бы _ в названии, это не правильно
4. В функции read_pdf_text, pdfplumber.open(path) открывал файл, но не закрывал его
5. Также, не было проверки на наличие страниц.
6. Заменил .text на extract_text()